Use the example code as a base for your task.

1. create new route and controllers for register. use ``` user``` as an entity. 
2. create a new route and controllers for login. use ``` user``` as an entity.
