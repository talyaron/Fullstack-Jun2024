convert the project in ```05-CRUD/example-update``` to having the following structure and files:

```
06-Routes/followup/<your-name>
* src
    * routes
        * usersRoutes.ts
    * models
        * usersModel.ts

```